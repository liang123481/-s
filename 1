#include<iostream>
#include<vector>
using namespace std;
int flag =0;
bool isfind[100] = {false};
void dfs(int n,vector<vector<int>>m) {
	if (isfind[n]) {
		flag = 1;
		return;
	}
	isfind[n] = 1;
	for (int i = 0; i < m[n].size(); i++) {
		dfs(m[n][i], m);
	}
	isfind[n] = 0;
	return;
}
int main() {
	int n, m;
	cin >> n >> m;
	int t1, t2;
	vector<vector<int>>list(n);
	while (m--) {
		cin >> t1 >> t2;
		list[t1].push_back(t2);
	}
	dfs(0, list);
	if (flag) {
		cout << "有环";
	}
	else {
		cout << "无环";
	}
}
